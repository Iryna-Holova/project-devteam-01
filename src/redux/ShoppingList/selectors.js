export const selectIngredients = (state) => state.shoppingList.ingredients;
export const selectIsLoading = (state) => state.shoppingList.isLoading;
export const selectError = (state) => state.shoppingList.error;
